#include <stdio.h>
int main() {
 int a;
 printf("enter a number :");
 scanf("%d",&a);
 if (a%2==0 && a!=0)
 printf("even number");
 else if (a%2!=0 )
 printf("odd number");
 else
 printf("zero is neither even nor odd");
 return 0;
}
